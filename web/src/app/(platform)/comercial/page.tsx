import { UnderConstruction } from "@/components/platform/under-construction";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Comercial | Moovilog",
  description: "",
};

export default function MerchantPagePlatform() {
  return (
    <main>
      <UnderConstruction />
    </main>
  );
}
